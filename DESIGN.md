# DESIGN.md — 重賞スコープ

> 「重賞スコープ」のデザイン仕様書。
> コンセプト: **週末の重賞だけを載せる一枚の専門紙**。媒体は Web だが、版面の規律は紙の競馬新聞に置く — 生成りの紙に墨のインク、細罫、詰まった組版、予想印(◎○▲△)、枠色。装飾のためのカードやシャドウは使わない。

---

## 1. デザイン原則

- **紙面である**: 背景は一枚の紙。要素は「カード」ではなく「罫で区切られた欄」。`border-radius: 0`、`box-shadow` なし。区切りはすべて細罫(1px)
- **色はデータが持つ**: ページの彩りは装飾ではなく、枠色(8色)・予想印・グレード章から生まれる。地の色数は最小限に保つ
- **印の言語**: 予想順位は数字より先に「印」で語る。◎(1位)○(2位)▲(3位)△(4–5位)
- **ライトのみ**: 紙のアイデンティティのため、ダークモードは意図的に非対応。全色を明示指定する

## 2. カラートークン

### 地と墨

| 名前 | Hex | 用途 |
|---|---|---|
| 紙 `--paper` | `#FAF7F0` | ページ背景 |
| 紙(欄) `--paper-inset` | `#F3EEE3` | テーブルヘッダー、選択行、hover |
| 墨 `--ink` | `#1C1A17` | 本文、見出し、罫の強 |
| 薄墨 `--ink-weak` | `#6E675C` | 補足、キャプション、単位 |
| 罫 `--rule` | `#D5CDBE` | 細罫(区切り線全般) |

### アクセント(使いどころを限定)

| 名前 | Hex | 用途 |
|---|---|---|
| 朱 `--shu` | `#C43B2E` | ◎、的中、回収率プラス、アクティブ状態。**1画面に朱の面積は1割未満** |
| 藍 `--ai` | `#33507A` | リンク、フォーカスリング、回収率マイナス、○印 |

回収率・収支は日本の相場慣習に合わせ **プラス=朱、マイナス=藍**(欧米式の緑/赤は使わない)。

### 枠色(JRA 慣習に固定。機能色であり変更禁止)

| 枠 | 地色 | 文字色 |
|---|---|---|
| 1 白 | `#FFFFFF`(罫色の枠線付き) | 墨 |
| 2 黒 | `#2B2B2B` | 白 |
| 3 赤 | `#D9333F` | 白 |
| 4 青 | `#005CAF` | 白 |
| 5 黄 | `#EFBB24` | 墨 |
| 6 緑 | `#227D51` | 白 |
| 7 橙 | `#ED6D3D` | 白 |
| 8 桃 | `#EE87B4` | 墨 |

### グレード章(バッジ)

- G1: 朱地 × 白抜き / G2: 藍地 × 白抜き / G3: 墨地 × 白抜き / OP・その他: 白地 × 墨 × 罫線囲み
- 形は角丸なしの小さな矩形。フォントは太字サンズ 11px、字間 0.08em

## 3. タイポグラフィ

@fontsource でセルフホスト(Pages 配信のため CDN 依存を避ける)。

| ロール | フェイス | ウェイト | 用途 |
|---|---|---|---|
| 題字・レース名 | **Shippori Mincho B1** | 700 / 800 | サイト題字、レース名、ページ見出し。ここ以外で明朝を使わない |
| 本文・UI | **IBM Plex Sans JP** | 400 / 500 / 700 | 本文、テーブル、ボタン、ラベル |
| 数値 | IBM Plex Sans JP + `font-variant-numeric: tabular-nums` | 500 / 700 | オッズ、スコア、回収率、タイム。**数値は必ず tabular** |

スケール(密度は専門紙寄り):

| Role | Size | LH | 備考 |
|---|---|---|---|
| 題字 | 28px | 1.2 | Shippori 800 |
| レース名(詳細) | 26px | 1.3 | Shippori 700 |
| 見出し | 18px | 1.4 | Plex 700 |
| 本文 | 14px | 1.7 | Plex 400 |
| データ表 | 13px | 1.5 | |
| キャプション・凡例 | 12px | 1.5 | 薄墨 |
| 大数値(回収率など) | 34px | 1.1 | tabular 700 |

## 4. シグネチャ: 馬柱(うまばしら)

**このサイトが記憶される一点**。レース詳細の出走馬一覧を、専門紙の馬柱と同じ**縦組みの列**で組む。

- 列は**馬番の右→左**(1番が最右。紙の出馬表と同じ向き)
- 1列 = 1頭、幅 64–72px、列間は細罫
- 列の構成(上から): 枠色チップ(馬番入り、24px角) → 予想印(20px、◎は朱) → 馬名(**`writing-mode: vertical-rl`** の縦書き、Shippori 700、15px) → 性齢・斤量(横書き小) → 騎手名(縦書き 11px 薄墨) → 単勝オッズ(tabular) → 総合スコア(数値+列下部の縦インクバー: 高さ=スコア比、色は墨、1位のみ朱)
- 列クリックで選択状態(地が `--paper-inset`)になり、直下の「ファクター欄」にその馬の8ファクター内訳を表示
- **モバイル(<768px)では縦組みを捨て**、1頭=1行の横テーブルにフォールバック(印・チップ・スコアバーは維持)

## 5. ページ構成

共通シェル: ヘッダーは細罫1本で仕切る。題字「重賞スコープ」(Shippori 800)+ 右端にナビ「番組表 / 的中実績」+ データ更新日時(12px 薄墨)。コンテナ max-width 1080px。フッターに免責と出典(§8)。

### 5-1. 番組表(Dashboard)

```
┌────────────────────────────────────────┐
│ 8月30日(土)                    ← 日付見出し(Shippori 18px)
│ ──────────────────────────────────────
│ [G2] 新潟記念        新潟 芝2000m 良 ☀   ◎ ホースネーム
│ ──────────────────────────────────────
│ [G3] キーンランドC   札幌 芝1200m 稍重 ☁  ◎ ホースネーム
│ ──────────────────────────────────────
│ 8月31日(日)
│ ──────────────────────────────────────
│ ...
└────────────────────────────────────────┘
```

- 1レース=1行。カード化しない。行 hover で地が `--paper-inset`
- 行の右端に本紙の◎(予想1位馬名)を予告として出す
- レース未取得・予想未生成の空状態は「まだ紙面がありません」ではなく行動を示す: 「次回更新: 土曜 6:00。手動で取得する場合はデータ取得を実行してください」(ローカル環境のみ取得ボタン表示。公開ビルドでは非表示)

### 5-2. レース詳細(RaceDetail)

```
┌────────────────────────────────────────┐
│ [G2] 新潟記念            2026年8月30日(日) 新潟11R
│ 芝2000m ・ 良 ・ 晴                ← 条件行(13px)
│ ════════════════════════════════════  ← 太罫(2px 墨)
│ 〔馬柱〕 ←§4 のシグネチャ。右から馬番順
│ ──────────────────────────────────────
│ 〔ファクター欄〕選択馬の8ファクター横バー
│   近走成績 ████████░░ 82.0(寄与 16.4)
│   コース適性 ██████░░░░ 61.5(寄与 9.2) …
│ ──────────────────────────────────────
│ 〔印一覧〕◎○▲△の馬と総合スコアの順位表(コンパクト)
└────────────────────────────────────────┘
```

- ファクター表示は Chart.js レーダーを廃止し、**CSS 横バー**(墨バー、地は罫色)に置き換える(紙面の語彙に合わせる+依存削減)
- バーは全ファクター 0–100 の生スコアと重み付き寄与を併記

### 5-3. 的中実績(新設ページ)

```
┌────────────────────────────────────────┐
│ 的中実績                     集計期間 2026/9/5〜
│ ──────────────────────────────────────
│  単勝回収率      複勝回収率     ◎複勝率    対象
│   112.4%          89.1%        41.2%     34R
│  (34px 朱/藍)   (34px)       (34px)    (薄墨)
│ ──────────────────────────────────────
│ 〔累計収支グラフ〕Chart.js 折れ線(墨の細線・塗りなし・
│   基準線100%を罫色破線で。プラス圏の線のみ朱)
│ ──────────────────────────────────────
│ 〔回顧表〕1行=1レース:
│  日付 | レース | ◎馬名 | 結果(着) | 単勝払戻 | 複勝払戻 | 収支
│  的中行は払戻セルを朱、不的中は「―」
└────────────────────────────────────────┘
```

### 5-4. 馬詳細(HorseDetail)

戦績表を紙の様式に統一(細罫・tabular 数値・馬場と着順列)。着順1–3着は墨太字、1着のみ朱。

## 6. モーション(1箇所だけ)

- レース詳細の初回表示で、予想印が**捺印されるように**入る: `scale(1.15)→1` + opacity、150ms、馬柱の右から 40ms ずつステートガー
- `prefers-reduced-motion: reduce` では無効化
- それ以外のアニメーションは hover の背景遷移(100ms)のみ。追加しない

## 7. 品質フロア

- キーボードフォーカス: `outline: 2px solid var(--ai); outline-offset: 2px`
- 枠色チップ・予想印に aria-label(例: `aria-label="4枠8番"`、`aria-label="本命"`)
- テーブルは semantic な `<table>`/`<th scope>`。馬柱は `role` と読み上げ順を横組み相当に保つ
- レスポンシブ: 1080px 固定 → 768px 未満で馬柱フォールバック(§4)・番組表は折返し
- コントラスト: 本文 墨/紙 = 13:1。薄墨/紙 = 4.6:1(AA)

## 8. コピー規約

- 用語は専門紙の語彙を使い、初出で補わない専門語は避ける: 番組表 / 馬柱 / 本紙見解 / 的中実績 / 回顧
- ボタンは動作をそのまま書く: 「データ取得を実行」「予想を見る」。「送信」「OK」禁止
- エラーは原因と次の行動: 「データを取得できませんでした。時間をおいて再実行してください」
- フッター(全ページ共通・12px 薄墨):
  「本サイトの予想は独自スコアによる参考情報であり、的中を保証するものではありません。投資助言ではありません。/ データ出典: netkeiba.com・JRA / 最終更新: {timestamp}」

## 9. 実装トークン(tailwind.config.js)

SmartHR トークン(旧 DESIGN.md)は全廃し、上記を `--paper` 等の CSS 変数 + Tailwind theme extend で定義し直す。既存の `card-smarthr` / `btn-primary` 等のユーティリティクラスは削除し、`rule-b`(下罫)、`chip-waku-{1..8}`、`mark`(印)、`badge-grade-{g1,g2,g3,op}` 等の紙面語彙に置き換える。
